# Quick Deploy

## Docker (local, free)
```bash
cp env_template.txt .env
docker compose up -d --build
```

## Streamlit Cloud (dashboard)
- App path: `web_dashboard.py`
- Add `GEMINI_API_KEY` in secrets


