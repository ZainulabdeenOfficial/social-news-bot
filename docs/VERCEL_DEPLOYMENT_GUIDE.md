# Vercel Deployment (API Only)

- Keep `api/` at project root for serverless endpoints.
- Configure env vars in Vercel dashboard (use `GEMINI_API_KEY`).
- Dashboard (`web_dashboard.py`) is for local use; Vercel hosts API pages.


